import type { Props } from "@theme/DocVersionBadge";

export default function DocVersionBadge({
  className,
}: Props): JSX.Element | null {
  //Remove showing the version badge on every page
  return null;
}
