{
  "name": "watcher",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build-local": "esbuild src/standalone.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/standalone.js",
    "build-lambda": "esbuild src/index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
    "start": "node dist/standalone.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.556.0",
    "@aws-sdk/client-sqs": "^3.556.0",
    "@aws-sdk/util-dynamodb": "^3.556.0",
    "aws-lambda": "^1.0.7",
    "date-fns": "^3.6.0",
    "graphql": "^16.8.1",
    "graphql-http": "^1.22.1"
  },
  "devDependencies": {
    "@github/prettier-config": "^0.0.6",
    "@types/aws-lambda": "^8.10.137",
    "@types/node": "^20.12.7",
    "esbuild": "^0.20.2",
    "nodemon": "^3.1.0",
    "prettier": "^3.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "prettier": "@github/prettier-config"
}
